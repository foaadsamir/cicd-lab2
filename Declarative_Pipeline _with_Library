@Library('jenkins-library')_

pipeline {
    agent any
    stages {
        stage('Build with Library') {
            steps {
                script {
                    mavenBuild()
                }
            }
        }
    }
}
